#include <iostream>
#include "math.h"
using namespace std;

int main()
{
    int x = 0,y = 0,z = 0;
    cin  >> x >> y >> z;
    cout << x <<" "<< pow(x , 2) <<" "<< pow(x , 3) << endl;
    cout << y <<" "<< pow(y , 2) <<" "<< pow(y , 3) << endl;
    cout << z <<" "<< pow(z , 2) <<" "<< pow(z , 3) << endl;
    return 0;
}
